{% extends 'base.html.twig' %}

{% block content %}


<div class="container">
    <h1> Modifier mon mot de passe </h1>

   {{ form(form)}}

    <hr>
    {% if notification %}
    <div class= "alert alert-info"> {{notification}} </div>
    {% endif %}
    <a href="{{ path('account')}}"> Retourner dans mon compte</a>
    <hr>

</div>
{% endblock %}
